mixin alert-success(time)
  - if(messages.success)
      .message.info
      div(
        class= "alert alert-success"
        data-time=time
        show-alert
      ) #{messages.success} <span close-alert>x</span>
      //- message cua flass la bien globol nen ko can truyen 